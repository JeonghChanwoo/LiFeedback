# Copy these values into your local shell environment; do not commit real tokens.
GITHUB_TOKEN=
PORT=8787
HOST=0.0.0.0
ALLOW_NULL_ORIGIN=true
# Set this when serving the HTML from a local web server instead of file://.
# ALLOWED_ORIGINS=http://localhost:8000
