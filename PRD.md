# LiFeedback 제품 요구사항 문서 (PRD)

## 1. 문서 목적

LiFeedback은 사용자가 하루 일과를 기록하고 장기 목표와 비교한 AI 피드백을 받는 브라우저 기반 일과표다. 이 문서는 현재 저장소에 구현된 제품의 요구사항과 범위를 정의하는 제품 기준 문서(Source of Truth)다.

## 2. 제품 목표

- 24시간 일과를 10분 단위로 빠르게 기록한다.
- 사용자 프로필과 장기 계획을 바탕으로 현실적인 하루 계획을 생성한다.
- 실제 일과와 계획을 비교해 점수, 원인, 개선 방안을 제공한다.
- 반복 사용을 통해 행동 기록을 남기고 장기 계획을 조정한다.
- 별도 데이터베이스 없이 브라우저 localStorage로 개인 기록을 유지한다.

## 3. 사용자와 사용 시나리오

### 주요 사용자

자신의 생활 패턴을 관찰하고 목표에 맞는 계획을 세우려는 개인 사용자다. 사용자는 브라우저에서 프로필, 계획, 일과, 소감과 Todo를 직접 관리한다.

### 핵심 시나리오

1. 사용자가 처음 앱을 열고 온보딩에서 이름, 생년월일, 직업, 목표, 현재 상황과 계획을 입력한다.
2. 사용자가 날짜를 선택하고 시간표의 10분 칸을 드래그해 활동을 기록한다.
3. 사용자가 미래 날짜의 계획과 Todo를 작성하거나 AI로 하루 계획을 생성한다.
4. 사용자가 제출하면 AI가 계획 대비 일치도와 피드백을 생성하고, 결과를 해당 날짜 기록에 저장한다.
5. 사용자가 월간 보기에서 날짜별 점수를 확인한다.
6. 사용자가 최근 행동 기록을 바탕으로 장기 계획 업데이트를 요청한다.

## 4. 기능 요구사항

### 4.1 일과 기록

- 하루를 24시간, 10분 단위 144개 슬롯으로 표시해야 한다.
- 사용자는 연속 또는 복수 슬롯을 드래그해 활동을 입력할 수 있어야 한다.
- 활동별 색상과 시간 범위를 표시해야 한다.
- 날짜 이동, 오늘 이동, 과거·미래 날짜 조회를 지원해야 한다.
- 과거 기록은 조회 중심으로 제공하고, 미래 날짜는 계획과 Todo를 작성할 수 있어야 한다.

### 4.2 프로필과 온보딩

- 이름, 생년월일, 직업, 목표, 현재 상황, 장기 계획을 입력·수정할 수 있어야 한다.
- 최초 방문 시 온보딩 화면을 제공해야 한다.
- 프로필과 온보딩 완료 상태는 브라우저 localStorage에 저장해야 한다.

### 4.3 계획과 Todo

- 미래 날짜에 계획 항목 이름과 시간을 추가·삭제할 수 있어야 한다.
- 자주 사용하는 일과 프리셋을 관리하고 활동 입력 시 재사용할 수 있어야 한다.
- AI 하루 계획 생성 결과를 계획 목록에 추가할 수 있어야 한다.
- 장기 계획 생성 결과와 요일별 반복 계획을 저장할 수 있어야 한다.

### 4.4 AI 기능

- 하루 계획 생성: 프로필, 현재 상황, 날짜, 요일, 프리셋을 사용해 4~8개 계획을 생성한다.
- 장기 계획 생성: 프로필과 프리셋을 사용해 반복 가능한 계획과 요일별 계획을 생성한다.
- 계획 업데이트: 최근 최대 30일의 실제 행동 기록과 기존 계획을 분석해 수정 계획을 생성한다.
- 피드백: 실제 일과, 장기 계획, 사용자의 소감을 바탕으로 0~100점 점수, 총평, 원인과 개선 방안을 생성한다.
- AI 기능이 실패해도 사용자의 일과표 다운로드 흐름은 중단하지 않아야 한다.

### 4.5 월간 보기와 도움말

- 월간 캘린더에서 날짜별 피드백 점수를 표시해야 한다.
- 사용자가 도움말을 열고 다시 보지 않기를 선택할 수 있어야 한다.
- 설정에서 프로필, 질문 제목, 프리셋을 관리할 수 있어야 한다.

### 4.6 데이터와 개인정보

- 앱 데이터는 현재 브라우저의 localStorage에 저장한다.
- 서버는 AI 요청 처리에 필요한 입력만 받아 응답하며, 애플리케이션 데이터베이스를 사용하지 않는다.
- GitHub 토큰은 사용자 브라우저나 저장소에 노출하지 않는다.

## 5. 품질 및 운영 요구사항

- 배포 환경에서 브라우저가 HTTPS로 앱에 접근할 수 있어야 한다.
- AI 프록시는 Azure App Service가 주입하는 `PORT`와 `0.0.0.0`에 바인딩해야 한다.
- `/health`는 프로세스 liveness를, `/ready`는 Copilot 인증 설정 여부를 제공해야 한다.
- 프록시는 허용된 origin만 CORS 요청을 허용해야 한다.
- 요청 본문 크기, 동시 AI 요청 수, IP별 요청 빈도를 제한해야 한다.
- 오류 응답에 인증 토큰이나 내부 오류 상세를 포함하지 않아야 한다.

## 6. 성공 기준

- 사용자가 온라인 주소에서 HTML을 열고 AI 기능을 사용할 수 있다.
- 정상적인 AI 요청이 계획 또는 피드백 JSON으로 반환된다.
- 잘못된 origin과 잘못된 Content-Type 요청이 거부된다.
- 새 브라우저에서 기존 서버 데이터베이스 없이 기본 흐름을 사용할 수 있다.
- Azure App Service 재시작 후에도 환경변수 기반 인증으로 `/ready`가 정상 응답한다.

## 7. 범위 밖

- 사용자 계정, 서버 동기화, 다중 기기 데이터 동기화
- 별도 데이터베이스와 관리자 콘솔
- GitHub 토큰을 포함한 클라이언트 측 인증
- 모바일 네이티브 앱
- AI 모델 또는 Copilot SDK를 대체하는 별도 모델 운영
